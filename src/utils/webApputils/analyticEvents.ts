/**
 * Trimmed mirror of amber-user-website's utils/webApputils/analyticEvents.
 * Only the constant CustomLink references is carried over.
 */
export const WEB_VIEW_APP_EVENTS = {
  OPEN_EXTERNAL_URL: "OPEN_EXTERNAL_URL",
};

export default WEB_VIEW_APP_EVENTS;
