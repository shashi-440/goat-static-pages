node_modules/
public/
*.log
.DS_Store
loadable-stats*.json
.vercel

# Local agent/tooling state — machine-specific, not part of the project.
.agents/
.claude/
skills-lock.json

# This repo uses yarn; a stray npm lockfile would let the two drift apart.
package-lock.json

dist/

# Local environment. Holds the Mapbox token — see src/config/index.ts.
.env
.env.*
!.env.example
